#include<iostream>
#include<unordered_set>
using namespace std;

//unique order not sorted
//O(1) 
//O(n) worst case which happens one in a blue moon

int main(){ 
    unordered_set<int> st;
    //rest same as set
    //except upper_bound and lower_bound
} 