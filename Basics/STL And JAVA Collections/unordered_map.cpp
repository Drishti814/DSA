#include<iostream>
#include<unordered_map>
using namespace std;

//unique order not sorted
//O(1) 
//O(n) worst case which happens one in a blue moon

int main(){ 
    unordered_map<int,int> mpp;
    //rest same as map(like unordered_set)
} 